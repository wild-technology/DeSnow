# DeSnow requirements
# Target: RTX 5090 (sm_120 / Blackwell) + CUDA 13.2 + Threadripper
#
# Install PyTorch with CUDA 13.0 support (compatible with CUDA 13.2 via
# NVIDIA minor version compatibility):
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
#
# Then install the rest:
#
#   pip install -r requirements.txt

torch>=2.10.0
torchvision>=0.25.0
opencv-python>=4.8.0
numpy>=1.24.0
albumentations>=1.3.0
pyyaml>=6.0
tqdm>=4.65.0
scikit-image>=0.21.0
matplotlib>=3.7.0
Pillow>=10.0.0
